{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Build",
      "program": "${workspaceRoot}/index.js",
      "cwd": "${workspaceRoot}"
    }
  ]
}